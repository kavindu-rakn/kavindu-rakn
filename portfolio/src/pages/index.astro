---
import { getWork } from '../lib/work';
import Layout from '../layouts/Layout.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import BlueprintHero from '../components/BlueprintHero.astro';
import ProjectCard from '../components/ProjectCard.astro';
import SkillsMatrix from '../components/SkillsMatrix.astro';
import ContactBlock from '../components/ContactBlock.astro';
import { SITE, ABOUT_PRECIS, elapsedPhrase } from '../consts';

/*
 * The six entries here are the same six leaves as the hero's schema tree, in
 * the same order. That is deliberate: the hero is a diagram of this grid.
 */
const work = await getWork();

const elapsed = elapsedPhrase();

/*
 * SchemaShift and TalentHub are the two flagship case studies and each spans two cells.
 * Single-column cards + About aside fill out the rest of the 2-column grid.
 */
const flagships = new Set(['schemashift', 'talenthub']);
const singleCardCount = work.filter((w) => !flagships.has(w.id)).length;
const asideFillsRow = (singleCardCount + 1) % 2 === 1;
---

<Layout title={SITE.name}>
  <SiteHeader slot="header" current="/" />

  <BlueprintHero />

  <section id="work" class="border-t-2 border-figure">
    <div class="mx-auto max-w-6xl px-6 py-20 sm:py-28">
      <header
        class="flex flex-wrap items-baseline justify-between gap-x-6 gap-y-2 border-b-2 border-figure pb-4"
      >
        <h2
          class="font-display text-2xl font-extrabold tracking-[-0.02em] uppercase sm:text-3xl"
        >
          Selected work
        </h2>
        <p class="annotation">{work.length} sheets</p>
      </header>

      {/*
        `items-start` rather than the default stretch. Stretching made every card
        in a row as tall as the tallest, and the `mt-auto` footers then left a
        void above the tech stack on any entry with fewer highlights — Luna's was
        a chasm. Sizing each card to its own content removes it without dropping
        any content from the shorter ones.
      */}
      <div class="mt-10 grid items-start gap-6 md:grid-cols-2">
        {
          work.map((entry) => (
            <ProjectCard entry={entry} feature={flagships.has(entry.id)} />
          ))
        }

{/*
          Not a project card. It carried a sheet number and the same header rule
          as the six beside it, so it read as a seventh project called "Twelve
          months". The number is gone, the ground is raised, and the heading sets
          smaller than a project title.
        */}
        <aside
          class:list={[
            'flex min-w-0 flex-col border-2 border-accent card-solid p-6 sm:p-8',
            asideFillsRow && 'md:col-span-2',
          ]}
        >
          <p class="annotation">About</p>

          <h3 class="mt-5 font-display text-2xl font-extrabold tracking-[-0.02em] sm:text-3xl">
            {elapsed} of it
          </h3>

          <p class="mt-4 text-figure-muted">
            My first commit was on 17 August 2025.
          </p>

          <p class="mt-4 text-sm leading-relaxed">{ABOUT_PRECIS}</p>

          <footer class="mt-6 border-t border-rule pt-4">
            <a class="rule-link rule-link--internal" href="/about">Read the rest</a>
          </footer>
        </aside>
      </div>
    </div>
  </section>

  {/*
    Capabilities and the ask both sit AFTER the work, not before it. The work is
    the argument; pushing it further down the page to make room for a skills
    list would trade the strong thing for the summary of it.
  */}
  <section class="border-t-2 border-figure">
    <div class="mx-auto max-w-6xl px-6 py-20 sm:py-28">
      <SkillsMatrix />
    </div>
  </section>

  <section class="border-t-2 border-figure">
    <div class="mx-auto max-w-6xl px-6 py-20 sm:py-28">
      <ContactBlock />
    </div>
  </section>

  <SiteFooter slot="footer" compact />
</Layout>

